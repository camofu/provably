# Config for the tlsn/reseller prover, loaded via dotenv (it searches up from the
# working directory, so the repo-root `.env` is found from `tlsn/reseller`).
# Copy this to `.env` at the repo root and fill in your key. `.env` is gitignored.
#
# The reseller is upstream-agnostic; the values below proxy Anthropic as an example.

# The TLS server to proxy and attest (required).
UPSTREAM_HOST=api.anthropic.com

# Secret API key, injected as the x-api-key header and redacted from the proof.
UPSTREAM_API_KEY=sk-ant-your-key-here

# Extra request headers to inject, "Name: Value; Name2: Value2".
# Quote the value — dotenv won't parse an unquoted value containing spaces/colons.
UPSTREAM_HEADERS="anthropic-version: 2023-06-01"

# Optional reseller tuning. All have working defaults (shown), so leave them
# commented out unless you need to change them.
#
# RPC_URL=https://rpc.moderato.tempo.xyz   # Tempo RPC the reseller settles on
# PRICE=0.05                               # price per call, human units
# NOTARY_ADDR=127.0.0.1:7047               # where the notary process listens
# NOTARY_SEED=demo-notary-key              # notary key seed; only exposes /notary/pubkey
# MPP_SECRET_KEY=reseller-example-secret   # server secret for stateless 402 challenge ids
# RESELLER_MODE=honest                     # honest | cheat-substitute (demo fraud)
